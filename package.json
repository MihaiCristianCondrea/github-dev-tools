{
	"name": "github-dev-tools",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"test": "node --test --experimental-strip-types \"tests/**/*.test.ts\"",
		"dev": "vite",
		"validate:locales": "node scripts/validate-locales.mjs",
		"build": "tsc && vite build",
		"check": "npm run validate:locales && npm run build && npm test",
		"preview": "vite preview"
	},
	"devDependencies": {
		"typescript": "^7.0.2",
		"vite": "^8.2.0"
	},
	"dependencies": {
		"@material/web": "^2.5.0"
	}
}
